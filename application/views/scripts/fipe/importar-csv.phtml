<div class="container mt-4">
	<h4>Importar tabela FIPE (CSV)</h4>
	<p class="text-muted">Faça upload do arquivo CSV mensal da FIPE. O conteúdo anterior será substituído completamente.</p>
	<hr>

	<?php if ($this->mensagem): ?>
		<div class="alert alert-success"><?= $this->escape($this->mensagem) ?></div>
	<?php endif; ?>

	<?php if ($this->erro): ?>
		<div class="alert alert-danger"><?= $this->escape($this->erro) ?></div>
	<?php endif; ?>

	<form method="post" enctype="multipart/form-data" action="/fipe/importar-csv">
		<div class="mb-3">
			<label class="form-label">Arquivo CSV da FIPE</label>
			<input type="file" name="csv" class="form-control" accept=".csv,.txt" required>
			<div class="form-text">Formato esperado: Type, Brand Code, Brand Value, Model Code, Model Value, Year Code, Year Value, Fipe Code, Fuel Letter, Fuel Type, Price, Month</div>
		</div>
		<div class="alert alert-warning py-2">
			<strong>Atenção:</strong> todos os dados da tabela FIPE atual serão substituídos pelos do arquivo enviado.
		</div>
		<button type="submit" class="btn btn-primary">Importar</button>
		<a href="javascript:history.back()" class="btn btn-secondary ms-2">Cancelar</a>
	</form>
</div>
